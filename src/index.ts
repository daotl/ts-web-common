/**
 * Returns greeting to someone.
 *
 * @remarks
 * For more information about this starter template, see: {@link https://github.com/daotl/ts-lib-starter}.
 *
 * @param name - Name of someone
 * @returns Greeting to `name`
 */
export function hello(name: string): string {
  return `Hello ${name}!`
}
