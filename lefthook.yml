pre-commit:
  commands:
    biome:
      glob: '*'
      run: echo {staged_files} | tr ' ' '\n' | grep \\.[cm]\\?[jt]s$ | tr '\n' ' ' | xargs -i sh -c 'echo npx biome check --fix {}' && git add {staged_files}

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit $1
